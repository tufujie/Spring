接口输入参数要求
（1）每一个参数有独立的业务意义
不能使用json,里面包含一堆参数
各接口参数需要传入用户的都用userId， 各业务接口需要获取当前角色，在controller上下文中获取。
接口中id的用途，必须是明确语义的。 比如getNoticeDetail，那么接口参数id指的是Notice的id。
如果需要传入员工id，接口参数需要定义为employId
接口输出要求
（1）仅输出客户端需要的数据
  避免输出过大数据量。
  特别是接口是获取列表，只能输出主表内容，禁止输出分录内容，或者其他相关性的内容。
  比如：获取活动列表，禁止输出活动的评论分录内容

