为了查询文件数量， 写一个查询File表的子查询。
file表存放的是全部企业全部项目所有业务的图片文件的表。 表的数据两增长会非常快。
禁止任何查询 关联文件表查询。
遇到需要取图片数量的 这样的问题， 请设计解决方案。
包括 User表、Employee表、Customer表、Room表， 禁止任何查询 关联这表查询。
如果需要用到 （1）保存时，保存冗余字段到该表 （2）取数据时优先用缓存。
